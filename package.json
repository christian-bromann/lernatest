{
  "devDependencies": {
    "lerna": "^2.0.0-rc.4"
  },
  "scripts": {
    "install": "git submodule update --init --recursive && git submodule update --remote",
    "bootstrap": "lerna bootstrap"
  }
}
